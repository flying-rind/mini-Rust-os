[package]
name = "Ncore"
version = "0.1.0"
edition = "2024"

[dependencies]
bootloader_api = "0.11"
loader = { path = "../loader" }
hybrid-objects ={ path = "../hybrid-objects" }
hal = {path = "../hal"}
log = "0.4.27"

[features]
default = ["monolithic"]
hybrid = ["loader/hybrid"]
monolithic = ["loader/monolithic"]
